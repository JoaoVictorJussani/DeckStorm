<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Inscription</title>
    <link rel="shortcut icon" href="/images/icone.ico" type="image/x-icon">
    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      }
      body {
        background: #fff;
        color: #222;
        display: flex;
        flex-direction: column;
        align-items: center;
        min-height: 100vh;
        font-size: 1rem;
      }
      .container {
        width: 95%;
        max-width: 400px;
        background: #fff;
        padding: 2rem;
        border-radius: 12px;
        border: 2px solid #1976d2;
        box-shadow: 0 5px 15px rgba(25, 118, 210, 0.08);
        text-align: center;
        margin-top: 110px;
      }
      h2 {
        font-size: 2rem;
        margin-bottom: 1.5rem;
        color: #1976d2;
        font-weight: 700;
      }
      .error {
        color: #ff4d4d;
        margin-bottom: 1rem;
      }
      .form-group {
        margin-bottom: 1rem;
      }
      .form-actions {
        display: flex;
        justify-content: center;
      }
      label {
        display: block;
        margin-bottom: 0.5rem;
        color: #1976d2;
      }
      input {
        width: 100%;
        padding: 10px;
        border-radius: 5px;
        border: 2px solid #1976d2;
        font-size: 1rem;
        background: #fff;
        color: #222;
        margin-top: 10px;
        margin-bottom: 10px;
        transition: border 0.2s;
      }
      input:focus {
        border-color: #1565c0;
        outline: none;
      }
      button {
        background: #fff;
        color: #1976d2;
        border: 2px solid #1976d2;
        padding: 0.8rem 1.5rem;
        border-radius: 8px;
        cursor: pointer;
        font-size: 1.1rem;
        font-weight: 600;
        transition: background 0.3s ease, color 0.3s ease;
      }
      button:hover {
        background: #1976d2;
        color: #fff;
      }
      p {
        margin-top: 1rem;
        color: black;
      }
      a {
        color: #1976d2;
        text-decoration: none;
        font-weight: bold;
      }
      a:hover {
        color: #1565c0;
      }
      @media (max-width: 600px) {
        .container {
          margin-top: 80px;
          padding: 1rem;
        }
      }
    </style>
  </head>
  <body>
    @include('components/header_login')
    <div class="container">
      <h2>Inscription</h2>
      @if(flashMessages.has('error'))
        <div class="error">
          {{ flashMessages.get('error') }}
        </div>
      @endif
      <form action="/register" method="POST">
        {{ csrfField() }}
        <div class="form-group">
          <label for="username">Nom d'utilisateur:</label>
          <input type="text" id="username" name="username" required />
        </div>
        <div class="form-group">
          <label for="password">Mot de passe:</label>
          <input type="password" id="password" name="password" required />
          <p id="password-length-msg" style="color: #ff4d4d; font-size: 0.85em; margin-top: 0.3em; margin-bottom: 0;">
            le mot de passe doit avoir 8 caractères
          </p>
        </div>
        <div class="form-group">
          <label for="confirm_password">Confirmation du mot de passe:</label>
          <input type="password" id="confirm_password" name="confirm_password" required />
          <p id="password-match-msg" style="color: #ff4d4d; font-size: 0.85em; margin-top: 0.3em; margin-bottom: 0;">
            la vérification de mot de passe est correcte
          </p>
        </div>
        <div class="form-actions">
          <button type="submit" id="submit-button" style="display: none;">Inscription</button>
        </div>
      </form>
      <p>Vous avez déjà un compte ? <a href="/login">se connecter</a></p>
    </div>
    <script>
      document.addEventListener("DOMContentLoaded", function() {
        const usernameInput = document.querySelector("#username");
        const passwordInput = document.querySelector("#password");
        const confirmPasswordInput = document.querySelector("#confirm_password");
        const submitButton = document.querySelector("#submit-button");
        const passwordLengthMsg = document.getElementById("password-length-msg");
        const passwordMatchMsg = document.getElementById("password-match-msg");

        function validateForm() {
          const usernameValid = usernameInput.value.trim().length >= 3;
          const passwordValid = passwordInput.value.length >= 8;
          const passwordsMatch = passwordInput.value === confirmPasswordInput.value && passwordInput.value.length > 0;

          // Mot de passe longueur
          if (passwordValid) {
            passwordLengthMsg.style.color = "#00c853"; // vert
          } else {
            passwordLengthMsg.style.color = "#ff4d4d"; // rouge
          }

          // Vérification mot de passe
          if (passwordsMatch) {
            passwordMatchMsg.style.color = "#00c853"; // vert
          } else {
            passwordMatchMsg.style.color = "#ff4d4d"; // rouge
          }

          if (usernameValid && passwordValid && passwordsMatch) {
            submitButton.style.display = "block";
            submitButton.textContent = "Inscription";
          } else {
            submitButton.style.display = "none";
          }
        }
        [ usernameInput, passwordInput, confirmPasswordInput ].forEach(input => {
          input.addEventListener("input", validateForm);
        });
      });
    </script>
  </body>
</html>
