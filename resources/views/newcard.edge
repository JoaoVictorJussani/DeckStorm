<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Nouvelle Carte</title>
  <link rel="shortcut icon" href="/images/icone.ico" type="image/x-icon">
  <style>
    /* --- Styles globaux et navbar (issus de home.edge) --- */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    body {
      background: #fff;
      color: #222;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      font-size: 1rem;
    }
    body.theme-light {
      background: #fff;
      color: #222;
    }
    body.theme-dark {
      background: #0a0c10;
      color: #e0e0e0;
    }
    /* --- Titres harmonisés --- */
    h1 {
      font-size: 3rem;
      margin-bottom: 1.5rem;
      color: #1976d2;
      font-weight: 700;
      text-align: center;
    }
    /* --- Formulaire et messages --- */
    .main-content {
      margin-top: 120px;
      text-align: center;
      padding: 3rem 2rem;
      width: 100%;
    }
    .form-container {
      width: 95%;
      max-width: 600px;
      margin: auto;
      background: #fff;
      padding: 2rem;
      border-radius: 12px;
      border: 2px solid #1976d2;
      box-shadow: 0 5px 15px #1976d2;
    }
    body.theme-dark .form-container {
      background: #181c23;
      border-color: #1976d2;
      box-shadow: 0 5px 15px #1976d2;
    }
    label {
      font-size: 1.2rem;
      display: block;
      margin-top: 15px;
      color: #1976d2;
      text-align: left;
    }
    input, textarea {
      width: 100%;
      padding: 10px;
      border-radius: 5px;
      border: 2px solid #1976d2;
      font-size: 1rem;
      background: #fff;
      color: #222;
      margin-top: 5px;
      transition: background 0.2s, color 0.2s;
    }
    body.theme-dark input,
    body.theme-dark textarea {
      background: #23272f;
      color: #e0e0e0;
      border-color: #1976d2;
    }
    .submit-button {
      background: #fff;
      color: #1976d2;
      border: 2px solid #1976d2;
      padding: 0.8rem 1.5rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.1rem;
      font-weight: 600;
      margin-top: 20px;
      transition: background 0.3s ease, color 0.3s ease;
    }
    .submit-button:hover { background: #1976d2; color: #fff; }
    .abandon-button {
      background: #fff;
      color: #ff4d4d;
      border: 2px solid #ff4d4d;
      padding: 0.8rem 1.5rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.1rem;
      font-weight: 600;
      margin-top: 20px;
      transition: background 0.3s ease, color 0.3s ease;
    }
    .abandon-button:hover { background: #ff4d4d; color: #fff; }
    body.theme-dark .submit-button {
      background: #23272f;
      color: #1976d2;
      border-color: #1976d2;
    }
    body.theme-dark .submit-button:hover {
      background: #1976d2;
      color: #fff;
    }
    body.theme-dark .abandon-button {
      background: #23272f;
      color: #ff4d4d;
      border-color: #ff4d4d;
    }
    body.theme-dark .abandon-button:hover {
      background: #ff4d4d;
      color: #fff;
    }
    .message.error {
      margin-bottom: 1rem;
      padding: 1rem;
      border-radius: 5px;
      text-align: center;
      background-color: #ff4d4d;
      color: #fff;
    }
    .message.success {
      margin-bottom: 1rem;
      padding: 1rem;
      border-radius: 5px;
      text-align: center;
      background-color: #4caf50;
      color: #fff;
    }
  </style>
</head>
<body>
  <!-- Barre de navigation -->
  @include('components/header')

  <!-- Contenu principal -->
  <div class="main-content">
    <h1>Ajouter une nouvelle carte</h1>
    <form action="{{ route('cards.store', { deckId: deck.id }) }}" method="POST">
      {{ csrfField() }}
      @if (flashMessages.has('error'))
        <div class="message error">
          {{ flashMessages.get('error') }}
        </div>
      @endif
      @if (flashMessages.has('success'))
        <div class="message success">
          {{ flashMessages.get('success') }}
        </div>
      @endif
      <div class="form-container">
        <label for="question">Question :</label>
        <input type="text" id="question" name="question" value="{{ flashMessages.get('old.question', '') }}" />
        <label for="answer">Réponse :</label>
        <textarea id="answer" name="answer">{{ flashMessages.get('old.answer', '') }}</textarea>
        <div class="button-container">
          <button type="submit" class="submit-button">OK</button>
          <button type="button" class="abandon-button" onclick="window.location.href='{{ route('decks.show', { id: deck.id }) }}'">Abandonner</button>
        </div>
      </div>
    </form>
  </div>
</body>
</html>
