<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Question</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #000;
      color: #e0e0e0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      font-size: 1rem;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 1.5rem;
      color: #00adb5;
      font-weight: 700;
      text-align: center;
    }

    .card-container {
      perspective: 1000px;
      margin: 10px 0;
      display: inline-block;
      position: relative;
    }

    .card {
      width: 300px;
      height: 150px;
      position: relative;
      transform-style: preserve-3d;
      transition: transform 0.6s;
      cursor: pointer;
    }

    .card.is-flipped {
      transform: rotateY(180deg);
    }

    .card-face {
      position: absolute;
      width: 100%;
      height: 100%;
      backface-visibility: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 2px solid #00adb5;
      border-radius: 10px;
      background: #000;
      color: #e0e0e0;
      font-size: 1.2rem;
      text-align: center;
    }

    .card-face.front {
      background: #00adb5;
      color: #000;
    }

    .card-face.back {
      transform: rotateY(180deg);
    }

    button {
      background: #00adb5;
      color: #000;
      border: 2px solid #00adb5;
      padding: 0.8rem 1.5rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.2rem;
      font-weight: 600;
      transition: background 0.3s ease, color 0.3s ease;
      margin-top: 20px;
    }

    button:hover {
      background: #008c95;
      color: white;
    }
  </style>
</head>
<body>
  <h1>Question {{ questionIndex + 1 }} sur {{ deck.cards.length }}</h1>

  <div class="card-container">
    <div class="card" onclick="flipCard(this)">
      <div class="card-face front">
        {{ card.question }}
      </div>
      <div class="card-face back">
        {{ card.answer }}
      </div>
    </div>
  </div>

  <form 
    action="{{ questionIndex + 1 < deck.cards.length 
      ? route('exercise.presentQuestion', { deckId: deck.id, questionIndex: questionIndex + 1 }) 
      : route('exercise.finish', { deckId: deck.id }) }}" 
    method="{{ questionIndex + 1 < deck.cards.length ? 'GET' : 'POST' }}"> <!-- Use POST for "Terminer" -->
    {{ csrfField() }} <!-- Include CSRF token for POST requests -->
    <input type="hidden" name="mode" value="basic" /> <!-- Pass the mode -->
    <button type="submit">{{ questionIndex + 1 < deck.cards.length ? 'Suivant' : 'Terminer' }}</button>
  </form>

  <script>
    function flipCard(cardElement) {
      cardElement.classList.toggle('is-flipped');
    }
  </script>
</body>
</html>
