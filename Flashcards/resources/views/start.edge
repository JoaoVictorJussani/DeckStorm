<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Commencer l'exercice</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #000;
      color: #e0e0e0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      font-size: 1rem;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 2rem;
      color: #00adb5;
      font-weight: 700;
      text-align: center;
    }

    button {
      background: #00adb5;
      color: #000;
      border: 2px solid #00adb5;
      padding: 0.8rem 1.5rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.2rem;
      font-weight: 600;
      transition: background 0.3s ease, color 0.3s ease;
    }

    button:hover {
      background: #008c95;
      color: white;
    }
  </style>
</head>
<body>
  <h1>Commencer l'exercice pour le deck "{{ deck.title }}"</h1>
  <form action="{{ route('exercise.presentQuestion', { deckId: deck.id, questionIndex: 0 }) }}" method="GET" onsubmit="handleSubmit(this)">
    <input type="hidden" name="startTime" id="startTime" />
    <input type="hidden" name="results" value="[]" /> <!-- Initialize results array -->
    <label for="mode-select" style="margin-bottom: 1rem; font-size:1.2rem;">Choisissez un mode :</label>
    <select id="mode-select" name="mode" style="background: #00adb5; color: #000; border: 2px solid #00adb5; padding: 0.8rem 1.5rem; border-radius: 8px; cursor: pointer; font-size: 1.2rem; font-weight: 600; transition: background 0.3s ease, color 0.3s ease; margin-bottom: 1.5rem;">
      <option value="chronometre">Mode chronométré</option>
      <option value="basique">Mode basique</option>
      <option value="jusquauboutiste">Mode jusqu'au-bout-iste</option>
    </select>
    <button type="submit">C'est parti</button>
  </form>
  <script>
    function handleSubmit(form) {
      const mode = form.mode.value;
      if(mode === 'chronometre'){
        document.getElementById('startTime').value = Date.now();
      }
      return true;
    }
  </script>
</body>
</html>
