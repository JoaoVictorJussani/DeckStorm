<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>HomePage</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #000;
      color: #e0e0e0;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      font-size: 1rem;
    }

    .navbar {
      width: 100%;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: #000;
      border-bottom: 2px solid #00adb5;
      color: white;
      position: fixed;
      top: 0;
      left: 0;
      z-index: 10;
    }

    .navbar h2 {
      font-size: 1.8rem;
      font-weight: 700;
    }

    .navbar button {
      background: #00adb5;
      color: white;
      border: 2px solid #00adb5;
      padding: 0.7rem 1.2rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1rem;
      transition: background 0.3s ease;
    }

    .navbar button:hover {
      background: #008c95;
    }

    .navbar a {
      text-decoration: none;
      color: #00adb5;
      font-size: 1.5rem;
      font-weight: bold;
    }

    .main-content {
      margin-top: 120px;
      text-align: center;
      padding: 3rem 2rem;
      width: 100%;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 1.5rem;
      color: #00adb5;
      font-weight: 700;
    }

    .decks-container {
      width: 95%;
      max-width: 1200px;
      margin: auto;
    }

    table {
      width: 100%;
      text-align: left;
      background: #000;
      border: 2px solid #00adb5;
      border-radius: 10px;
      overflow: hidden;
    }

    th, td {
      padding: 15px;
      border-bottom: 1px solid #00adb5;
    }

    th {
      font-size: 1.4rem;
      font-weight: bold;
      background: #000;
      color: #00adb5;
      border-bottom: 2px solid #00adb5;
    }

    td {
      font-size: 1.2rem;
    }

    tr:hover {
      cursor: pointer;
    }

    .button-container {
      text-align: center;
      margin-top: 2rem;
      margin-bottom: 3rem;
    }

    .add-button {
      background: #000;
      color: #00adb5;
      border: 2px solid #00adb5;
      padding: 0.8rem 1.5rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.1rem;
      font-weight: 600;
      transition: background 0.3s ease, color 0.3s ease;
    }

    .add-button:hover {
      background: #00adb5;
      color: black;
    }

    /* Add red button styles for "Supprimer" and "Abandonner" */
    .abandon-button {
      background: #000;
      color: #ff4d4d;
      border: 2px solid #ff4d4d;
      padding: 0.8rem 1.5rem;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1.1rem;
      font-weight: 600;
      transition: background 0.3s ease, color 0.3s ease;
    }

    .abandon-button:hover {
      background: #ff4d4d;
      color: black;
    }

    .search-form {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 2rem;
      width: 100%;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    .search-input {
      flex: 1;
      padding: 0.8rem 1.2rem;
      border: 2px solid #00adb5;
      border-radius: 8px;
      background: rgba(0, 173, 181, 0.1);
      color: #e0e0e0;
      font-size: 1rem;
      transition: all 0.3s ease;
    }

    .search-input:focus {
      outline: none;
      background: rgba(0, 173, 181, 0.2);
      box-shadow: 0 0 0 3px rgba(0, 173, 181, 0.3);
    }

    .search-input::placeholder {
      color: #666;
    }

    .search-button {
      padding: 0.8rem 1.5rem;
      background: #00adb5;
      color: #000;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1rem;
      font-weight: 600;
      transition: all 0.3s ease;
      white-space: nowrap;
    }

    .search-button:hover {
      background: #008c95;
      transform: translateY(-1px);
    }

    /* Flash message styles */
    .flash-message {
      width: 100%;
      max-width: 800px;
      margin: 0 auto 2rem auto;
      padding: 1rem;
      border-radius: 8px;
      text-align: center;
      font-weight: 600;
      animation: fadeIn 0.3s ease;
    }

    .flash-success {
      background: rgba(0, 173, 181, 0.2);
      border: 2px solid #00adb5;
      color: #00adb5;
    }

    .flash-error {
      background: rgba(255, 77, 77, 0.2);
      border: 2px solid #ff4d4d;
      color: #ff4d4d;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="navbar">
    <a href="{{ route('home') }}">Flashcards</a>
    <div>
      <span>Bienvenue {{ user?.username }}</span>
      <form action="/logout" method="POST" style="display: inline;">
        {{ csrfField() }}
        <button type="submit" class="abandon-button">Se déconnecter</button>
      </form>
    </div>
  </div>

  <div class="main-content">
    @if(flashMessages.has('success'))
      <div class="flash-message flash-success">
        {{ flashMessages.get('success') }}
      </div>
    @endif

    @if(flashMessages.has('error'))
      <div class="flash-message flash-error">
        {{ flashMessages.get('error') }}
      </div>
    @endif

    <h1>Mes decks</h1>

    <div class="decks-container">
      <table>
        <thead>
          <tr>
            <th>Titre</th>
            <th>Description</th>
            <th>Cartes</th>
            <th>Visibilité</th> <!-- Add a new column for visibility -->
          </tr>
        </thead>
        <tbody>
          @if(userDecks.length)
            @each(deck in userDecks)
              <tr onclick="window.location.href='/deck/{{ deck.id }}'">
                <td><a style="text-decoration: none; color: white" href="/deck/{{ deck.id }}">{{ deck.title }}</a></td>
                <td>{{ deck.description }}</td>
                <td>{{ deck.cards.length }}</td> <!-- Display the number of cards -->
                <td>
                  <span title="{{ deck.visibility === 'public' ? 'Public' : 'Privé' }}">
                    {{ deck.visibility === 'public' ? '🔓' : '🔒' }}
                  </span>
                </td> <!-- Display lock icon based on visibility -->
              </tr>
            @endeach
          @else
            <tr>
              <td colspan="4">Aucun deck disponible.</td>
            </tr>
          @endif
        </tbody>
      </table>
    </div>

    <div class="button-container">
      <button class="add-button" onclick="window.location.href='{{ route('decks.create') }}'">+ Ajouter</button>
    </div>

    <div class="public-decks">
      <h2>Decks publics : {{ publicDecks.length }}</h2>
      <form action="{{ route('search.publicDecks') }}" method="GET" class="search-form">
        <input 
          type="text" 
          name="query" 
          placeholder="Rechercher un deck public..." 
          value="{{ query || '' }}"
          class="search-input"
        />
        <button type="submit" class="search-button">
          Rechercher
        </button>
      </form>
      <div class="decks-container">
        <table>
          <thead>
            <tr>
              <th>Titre</th>
              <th>Description</th>
              <th>Cartes</th>
              <th>Créé par</th>
            </tr>
          </thead>
          <tbody>
            @if(publicDecks.length)
              @each(deck in publicDecks)
                <tr onclick="window.location.href='/deck/{{ deck.id }}'">
                  <td><a style="text-decoration: none; color: white" href="/deck/{{ deck.id }}">{{ deck.title }}</a></td>
                  <td>{{ deck.description }}</td>
                  <td>{{ deck.cards.length }}</td>
                  <td>{{ deck.user.username }}</td> <!-- Display the creator's username -->
                </tr>
              @endeach
            @else
              <tr>
                <td colspan="4">Aucun deck public disponible.</td>
              </tr>
            @endif
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>
</html>
